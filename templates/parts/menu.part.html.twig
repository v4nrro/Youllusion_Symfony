<div class="container-fluid p-0 nav-bar">
    <nav class="navbar navbar-expand-lg bg-none navbar-dark py-3">
        <a href="/" class="navbar-brand px-lg-4 m-0">
            <h1 class="m-0 display-8 text-uppercase text-white">YOULLUSION</h1>
        </a>
        <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
            <div class="navbar-nav ml-auto p-4">

            {# SI NO HAY USUARIO #}
                {% if app.request.pathinfo=='/index' or app.request.pathinfo=='/' %}
                    <a href="/" class="nav-item nav-link active">Inicio</a>
                {% else %}
                    <a href="/" class="nav-item nav-link">Inicio</a>
                {% endif %}

                {% if app.request.pathinfo=='/contacto' %}
                    <a href="/contacto" class="nav-item nav-link active">Contacto</a>
                {% else %}
                    <a href="/contacto" class="nav-item nav-link">Contacto</a>
                {% endif %}

                {% if app.request.pathinfo=='/iniciar-sesion' %}
                    <a href="/iniciar-sesion" class="nav-item nav-link active">Iniciar Sesion</a>
                {% else %}
                    <a href="/iniciar-sesion" class="nav-item nav-link">Iniciar Sesion</a>
                {% endif %}

                {% if app.request.pathinfo=='/registro' %}
                    <a href="/registro" class="nav-item nav-link active">Registro</a>
                {% else %}
                    <a href="/registro" class="nav-item nav-link">Registro</a>
                {% endif %}
        
            {# SI HAY USUARIO #}
                {# {% if app.request.pathinfo=='/index' or app.request.pathinfo=='/' %}
                    <a href="/" class="nav-item nav-link active">Inicio</a>
                {% else %}
                    <a href="/" class="nav-item nav-link">Inicio</a>
                {% endif %} #}

                <a href="/publicaciones" class="nav-item nav-link">Publicaciones</a>

                <a href="/publicaciones/new" class="nav-item nav-link">Crear</a>

            {#
                <a href="/contacto" class="nav-item nav-link">Contacto</a>

                <a href="/mis-publicaciones/(ID_USUARIO)" class="nav-item nav-link">Mis Publicaciones</a>

                <a href="/perfil/(ID_USUARIO)" class="nav-item nav-link">Mi Perfil</a>

                <a href="/cerrar-sesion" class="nav-item nav-link">Cerrar Sesion (USUARIO)</a> #}

            {# SI HAY ADMIN #}
                {# <a href="/lista-usuarios" class="nav-item nav-link">Lista Usuarios</a> #}
            </div>
        </div>
    </nav>
</div>